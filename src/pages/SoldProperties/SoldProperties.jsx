import Layout from "../../containers/Layout/Layout"
import SoldPropertiesContainer from "../../containers/SoldPropertiesContainer/SoldPropertiesContainer"

const SoldProperties = () => {
  return (
    <Layout title="Sold / Rented Properties">
        <SoldPropertiesContainer />
    </Layout>
  )
}

export default SoldProperties
